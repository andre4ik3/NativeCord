// This is a small amount of code that is injected into the WebView before the
// Discord website itself is loaded. All it does is set up listeners for things
// that usually don't work in WKWebView so that we can handle them.

(function() {
    // Currently nothing
    // TODO: add notifications, file picker, etc...
})();
